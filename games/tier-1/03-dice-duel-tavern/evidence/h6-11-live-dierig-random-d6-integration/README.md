# H6.11 Live DieRig and Random d6 Evidence

This packet proves the bounded live integration of the approved H6.10 DieRig into the H6.9 Crooked Six tavern stage.

The captures cover the supported 1920×1080 and 1024×640 desktop contracts, player-side ready docking, full and reduced motion, faces 1/4/6, action locking, same-actor return, complete history, deterministic victory and defeat, development diagnostics, repeated turns, and concurrent input rejection.

`random-source-audit.json` records the Web Crypto rejection-sampling boundary without making a statistical fairness claim from a small sample. `protected-hashes.json` records the unchanged H6.10 laboratory sources. `capture-report.json` records browser console and page errors.

Human runtime review passed through the freshly packaged Academy `app.exe` in its intended repository/developer mode. The packaged Hub found the source workspace and intentionally hydrated Dice Duel through its managed local game server. Repeated full-motion rolls, post-settle Attack/Heal/Block unlocking, persistent same-actor reuse, terminal settle, victory at 1 HP, and the Academy route passed human review.

```text
humanRuntimeReviewPassed: true
runtimeApproved: true
packagedHubDeveloperModeAuditPassed: true
automatedWindowsControlAuditPassed: false
automatedWindowsControlAuditRequiredForClosure: false
productionDistributionModeTested: false
productionDistributionModeRequiredForH611: false
```

Repository/developer mode is an intentional open-source launcher path, not a packaging defect. Source-absent production distribution uses the separate catalog/Butler behavior and was neither tested nor required here. This packet does not approve H6.12 materials, action tokens, particles, final typography, or curriculum closure.
