# Tiny Goblin Academy H6.14 Repository Agent Guardrail Audit

## Status

Human-approved documentation lane implemented. Repository behavior unchanged.

```yaml
baseline: d53dd01868b6ec50348fa41eb510d338c00e2bf7
implementationBehaviorChanged: false
dualCheckoutImplemented: false
lfsMigrationExecuted: false
cardGoblinDuelStarted: false
```

## Purpose

Create a compact repository-entry authority for future coding agents while preserving detailed doctrine in its existing canonical records. The root `AGENTS.md` consolidates operational law; this audit records provenance, drift, precedence, and rationale.

## Files inspected

Repository instruction discovery and current-state audit inspected:

- every repository `AGENTS.md` path returned by complete discovery;
- `README.md`;
- `LOOP_FIRST_PROTOCOL.md`;
- `package.json`, `pnpm-workspace.yaml`, and `pnpm-lock.yaml` presence;
- `hub/package.json`;
- `hub/src-tauri/Cargo.toml`;
- `hub/src-tauri/rust-toolchain.toml`;
- `hub/src-tauri/.cargo/config.toml`;
- `hub/src-tauri/src/lib.rs` and current Hub runtime-status surfaces;
- `docs/workspace/TINY_GOBLIN_ACADEMY_WORKSPACE_INSTALL_ARCHITECTURE_PLAN.md`;
- `docs/runtime/TINY_GOBLIN_ACADEMY_H6_0_ACADEMY_SHELL_REFACTOR_AND_IN_GAME_HUD_PLAN.md`;
- `docs/runtime/TINY_GOBLIN_ACADEMY_H6_1_MINIMAL_RUNTIME_SHELL_CONTRACT.md`;
- `docs/runtime/TINY_GOBLIN_ACADEMY_HUB_EXTERNAL_BUILD_WORKSPACE_RESOLUTION.md`;
- `docs/evidence/TINY_GOBLIN_ACADEMY_EVIDENCE_STORAGE_AUDIT.md`;
- `docs/evidence/TINY_GOBLIN_ACADEMY_EVIDENCE_SIZE_INVENTORY.json`;
- `docs/planning/implementation/2026-07-19-tga-external-evidence-warehouse-plan.md`;
- `docs/assets/doctrine/asset-pipeline/TINY_GOBLIN_ACADEMY_ASSET_PIPELINE_CLI_LOCKDOWN_DOCTRINE.md`;
- `docs/assets/pipeline/workflows/TINY_GOBLIN_ACADEMY_ASSET_PROCESSING_WORKFLOW.md`;
- `docs/hub/runtime/TINY_GOBLIN_ACADEMY_LAUNCHER_RUNTIME_PLAN.md`;
- `docs/hub/runtime/TINY_GOBLIN_ACADEMY_HUB_RUNTIME_AND_PACKAGING_NOTES.md`;
- `docs/hub/runtime/TINY_GOBLIN_ACADEMY_H3_RUNTIME_DOCS_RECONCILIATION.md`;
- `docs/roadmap/TINY_GOBLIN_ACADEMY_TIER_1_5_VISUAL_INTEGRATION_CURRICULUM_PLAN.md`;
- `docs/roadmap/TINY_GOBLIN_ACADEMY_PHASE_1_5_STATUS.md`;
- `docs/architecture/TINY_GOBLIN_ACADEMY_REPO_HYGIENE_CLASSIFICATION.md`;
- `docs/manifests/TINY_GOBLIN_ACADEMY_MANIFEST_STRATEGY.md`;
- representative game-local `README.md`, `AGENT_TASKS.md`, playable-loop, human-review, playtest, and lessons records found by instruction/doctrine search.

No unrelated user directory or drive scan was performed.

## Existing AGENTS.md inventory and precedence

Complete repository discovery found no pre-existing `AGENTS.md` at any depth.

Several games contain `AGENT_TASKS.md`, but these are build/task records rather than Codex instruction-precedence files. They do not function as nested `AGENTS.md` authorities.

The new root `AGENTS.md` therefore applies repository-wide. A future nested `AGENTS.md` may strengthen or specialize its subtree but may not weaken root safety, Git, evidence, asset, authority, or human-review law.

## Duplicated rules found

The audit found correct rules repeated across multiple authorities:

- simulation owns truth while presentation adapts it;
- playable-loop scope precedes polish and later curriculum;
- human review is a distinct final gate;
- prepared/mapped assets are not runtime-approved;
- source assets remain untouched while cleanup writes derived outputs;
- exact-path staging is required and `git add .` is forbidden;
- pnpm and the root lockfile are canonical;
- source-present developer mode and source-absent production mode are distinct;
- evidence, manifests, hashes, validators, and provenance form separate authority layers.

These cross-cutting rules were promoted to root in compact form.

## Stale and contradictory instruction findings

No contradiction required mutation outside this lane. The following drift is historical and must not be mistaken for current operating truth:

1. `docs/workspace/TINY_GOBLIN_ACADEMY_WORKSPACE_INSTALL_ARCHITECTURE_PLAN.md` opens with a planning-era statement that no root workspace exists, while later sections record the completed root pnpm workspace and controlled install.
2. `docs/roadmap/TINY_GOBLIN_ACADEMY_PHASE_1_5_STATUS.md` describes a read-only Hub that does not launch games. H3/H6 runtime records and current Tauri source supersede that state.
3. `README.md` names evidence-size inventory and D-drive planning as the immediate next lane. H6.13B completed and approved that architecture record.
4. Early launcher and workspace docs say the Hub must not silently spawn dev servers. The current correction is more precise: the Hub may explicitly manage trusted local dev servers in source-present developer mode; production/distribution remains separate.
5. H6.1 preserves chronological “human review still required” and earlier build caveats before later final-review closure sections. These are historical phase records, not a current pending status.
6. `docs/manifests/TINY_GOBLIN_ACADEMY_MANIFEST_STRATEGY.md` retains planning-era “read-only Hub roster” language even though its manifest paths remain current authority.

The root file resolves operating precedence without rewriting historical records.

## Current Hub authority decision

Current runtime source and `TINY_GOBLIN_ACADEMY_HUB_EXTERNAL_BUILD_WORKSPACE_RESOLUTION.md` prove that a locally built Hub may live outside the repository, locate the manifest-validated C workspace, and launch workspace-backed games through Hub-managed local servers.

This is intentional developer behavior, not a packaging defect. Production actions remain explicitly unimplemented in current Tauri status. Agents must not collapse developer mode into the future source-absent distribution model or begin packaging remediation without authorization.

## Root rules promoted

The root authority now owns these repository-wide categories:

- current implementation status versus approved future architecture;
- simulation/presentation ownership;
- human-approval status separation;
- stage-first and curriculum boundaries;
- Git staging, commit, history, timeout, and divergence safety;
- evidence preservation and future migration boundaries;
- asset pipeline invariants;
- pnpm, Rust/Tauri, and dependency/build boundaries;
- Hub developer/production mode separation;
- repository/filesystem scope and encoding safety;
- executable agent behavior and explicit stop conditions.

## Rules intentionally left as references

The root file does not duplicate:

- H6.13 file counts, bytes, hashes, publication partitions, or physical volume identifiers;
- exact future migration commands, sparse patterns, helper implementation, or recovery algorithms;
- every registered asset cleanup method and provenance schema;
- every H5/H6 game lane or historical human-review decision;
- complete Hub process-management implementation;
- package census, install chronology, or Rust cache measurements;
- game-local contracts, coordinates, actor geometry, or visual recipes.

Those details remain with their committed evidence, implementation, runtime, asset, workspace, manifest, and game-local authorities.

## Current state versus future state

### Implemented now

- Canonical development remains in the current C checkout.
- Evidence authority remains the current ordinary-Git repository history.
- No D evidence checkout, dual-checkout helper, lane lock, evidence hook, LFS rule, LFS migration, or canonical C cutover exists.
- Card Goblin Duel visual integration has not begun.

### Approved but not implemented

- one repository and linear history;
- C-only source authority and push authority;
- restricted D evidence appliance;
- fast-forward-only synchronization and evidence-lane locking;
- LFS-cold C and evidence-object-materializing D;
- disposable migration clone;
- physical-volume identity guard;
- hard refusal on ambiguity or divergence;
- separately approved canonical cutover and old-clone retirement.

## Validation contract

H6.14 validation must prove:

- complete `AGENTS.md` discovery and unambiguous root scope;
- every root reference resolves;
- current and future evidence status agrees with H6.13B;
- package-manager and Hub-mode language agrees with current configuration/source;
- required Git, evidence, asset, filesystem, execution, and stop laws are present;
- changed Markdown is valid UTF-8 with no control characters, mojibake, trailing whitespace, or missing final newline;
- cached diff contains only the two approved documentation files;
- no package, lockfile, runtime, game, asset, manifest, evidence binary, LFS, Git configuration, clone, hook, or filesystem architecture changed.

## Exact files changed

- `AGENTS.md`
- `docs/runtime/TINY_GOBLIN_ACADEMY_H6_14_REPOSITORY_AGENT_GUARDRAIL_AUDIT.md`

No helper code, validator, hook, enforcement script, runtime behavior, dual-checkout implementation, LFS migration, or Card Goblin Duel work belongs to this lane.
