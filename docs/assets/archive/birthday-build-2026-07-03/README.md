# Birthday Build 2026-07-03 Archive

This folder preserves non-runtime scratch artifacts from the Level 8 Birthday Build cleanup pass.

Nothing here was deleted. Files were moved here to keep the repo root and Level 8 source folders readable while preserving Antigravity spike evidence and source backups for later review.

Contents:

- `debug-images/` — root-level debug PNGs and debug cell crops generated during asset extraction / cleanup probing.
- `source-backups/` — `.bak` copies of Level 8 source files created during the implementation spike.

These files are reference-only. Do not treat them as current runtime truth.
