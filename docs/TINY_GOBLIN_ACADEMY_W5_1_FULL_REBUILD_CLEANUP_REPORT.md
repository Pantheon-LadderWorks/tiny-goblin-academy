# Tiny Goblin Academy W5.1 Full Rebuild Cleanup Report

## Status

Full Dependency Cleanup + Root Reinstall Applied

## Purpose

* old per-game dependency caves and package-manager drift were removed;
* root pnpm workspace is now canonical;
* future developers should install from root;
* itch/player builds are not part of dependency workflow.

## Pre-Cleanup Baseline

* git status: clean
* pnpm version: 10.20.0
* pnpm store path: `C:\Users\kryst\AppData\Local\pnpm\store\v10`
* C drive free space before cleanup: 6.14 GB
* node_modules count before cleanup: 11 (including `.ignored` quarantines)
* dist count before cleanup: 10
* obsolete lockfiles found: 8 per-package `pnpm-lock.yaml` files, 2 `package-lock.json` drift files.

## Cleanup Applied

* workspace node_modules folders removed;
* generated dist folders removed;
* obsolete per-package pnpm lockfiles removed;
* package-lock drift removed.

## Post-Delete State

* C drive free space after cleanup: 6.69 GB (~560 MB reclaimed)
* remaining node_modules count before reinstall: 0
* remaining dist count before rebuild: 0
* remaining lockfile drift before reinstall: 0

## Reinstall

* command: `pnpm install --ignore-scripts`
* install result: Completed successfully in 12.1s
* warnings: None
* final dependency structure summary: The root workspace successfully downloaded and linked 163 packages across all 11 projects using native pnpm linkage.
* whether root lockfile changed: Unchanged

## Validation Results

* **academy manifest validator:** ✅ Passed
* **hub icon validator:** ✅ Passed
* **recursive tests:** ✅ Passed
* **recursive typecheck:** ✅ Passed
* **recursive build:** ✅ Passed

## Generated Outputs

* `dist/` folders were regenerated by build validation;
* they remain ignored and were not staged.

## Final Repo Architecture

* root pnpm lockfile is canonical;
* per-package lockfiles removed;
* npm package-lock drift removed;
* one root install path is expected for developers;
* package-level dependency structures are pnpm-managed after root install.

## Remaining Issues

None. The workspace is fully operational from the canonical root.

## Proposed Next Step

1. Review W5.1 report.
2. If validation passed, mark workspace migration complete.
3. Then return to launcher/install architecture, hub polish, or Level 1 planning.
